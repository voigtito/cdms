<div class="row row-cards">

  <div class="col-sm-6 col-lg-6">
    <div class="card p-3">
      <div class="d-flex align-items-center">
        <span class="stamp stamp-md bg-red text-white mr-3">
          <%= icon('fas', 'user-cog') %>
        </span>
        <div>
          <h4 class="m-0" data-toggle="tooltip" data-placement="bottom" title="Clique aqui para acessar a Ã¡rea administrativa">
            <%= link_to 'Área administrativa', '#' %>
          </h4>
          <small class="text-muted">Administradores</small>
        </div>
      </div>
    </div>
  </div>

  <div class="col-sm-6 col-lg-6">
    <div class="card p-3">
      <div class="d-flex align-items-center">
        <span class="stamp stamp-md bg-blue text-white mr-3">
          <%= icon('fas', 'user-cog') %>
        </span>
        <div>
          <h4 class="m-0" data-toggle="tooltip" data-placement="bottom" title="Clique aqui para acessar a Ã¡rea do cliente">
              <%= link_to 'Área do usuário', '#'  %>
          </h4>
          <small class="text-muted">Usuários</small>
        </div>
      </div>
    </div>
  </div>

<% if admin_signed_in? %>
  <%= link_to 'Sair', destroy_admin_session_path, method: :delete %>
<% else %>
  <%= link_to 'Entrar', new_admin_session_path %>
<% end %>

</div>
